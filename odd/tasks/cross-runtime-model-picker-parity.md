# Cross-runtime model picker parity

Objective: Complete the supported model configuration surfaces across OpenCode, Claude Code and Codex, using Pi/Gentle Shell as a reference without changing Pi or unsupported runtimes.

Problem: OpenCode's managed native `general` and `explore` agents cannot be edited in the picker; Claude's new RDD rows need short-terminal usability proof; Codex's custom picker omits distinct ODD and six RDD roles, while its isolated RDD CLI adapter ignores saved role models.

Authorized scope: Complete these gaps, preserve existing presets, fallback behavior, native review isolation and SDD support. Do not alter unrelated files or other runtimes. User requested one PR for all work with documented `size:exception`; remote push and PR to Gentleman-Programming/gentle-ai using the current gh/Git SSH session explicitly authorized. PR still requires an approved linked issue and target-host policy authority for protected labels. No remote work outside that destination/operation.

TDD: ODD mode unconfirmed (OpenSpec strict TDD is not ODD authority); run ordinary focused Go tests and functional harness checks. Delivery: single-pr, size exception explicitly requested; forecast >400 authored changed lines due to three runtime surfaces, tests, and review evidence. Not a license to omit tests or compress code. Branch feat/claude-native-rdd-models, base 21032461; prior Claude units aa96bec9 and 70bcc253 were native-approved and acknowledged in review-c6a1cb284be40c22. Engram mirror `odd/cross-runtime-model-picker-parity/tasks` pending if callable session-bound memory is unavailable.

- [x] P1: Expose OpenCode native general/explore as individually editable global picker rows. Preserve SDD/custom bulk actions, profile behavior and installed managed definitions. Tests: picker navigation, persistence/config read, overlay injection when absent/present. Route: delegated writer, multi-file preparation/write. Evidence: focused picker/injection tests passed; `env -u PI_CODING_AGENT_DIR go test ./internal/tui/screens ./internal/components/sdd -count=1` passed; `git diff --check` passed. Runtime boundary: temporary-config injection test (live OpenCode session N/A). Commit: `90e7ac5d`. Run with `PI_CODING_AGENT_DIR` unset to avoid the external-file incident.
- [x] P2: Make Claude custom RDD rows usable in short terminals; verify picker navigation and selection without changing named presets. Tests: cursor-following viewport at 12 lines covering six RDD rows and Confirm; focused Claude picker/TUI tests passed with `PI_CODING_AGENT_DIR` unset. Route: delegated writer, three-file edit. Commit: pending.
- [ ] P3: Establish Codex custom role policy from existing ODD classes and native CLI contract, then expose separate ODD Explorer/Worker/Verify and six RDD reviewer choices with persistence and safe defaults. Preserve named presets and SDD. Tests: picker/round-trip/persistence. Route: delegated mapper then writer.
- [ ] P4: Wire valid saved Codex RDD role models into actual isolated `codex exec --model` for four lenses, refuter and validator; preserve `--ignore-user-config`, sandbox and fallbacks. Tests: role selection, invalid/missing assignment, subprocess arguments, no isolation drift. Route: delegated writer.
- [ ] P5: Run focused and relevant broad checks, reconcile known SDD permission failure and CLI timeout without hiding failures; update work-unit evidence and independent assessment/review as applicable. Route: delegated verifier and parent native review.
- [ ] P6: Prepare one PR with documented over-budget rationale, approved issue, required type and size labels only under verified repository policy authority; no merge. Route: parent remote operations after checks.

Progress: P1 implemented and independently verified. Incident: inherited `PI_CODING_AGENT_DIR` caused a pre-existing safeguard test to write `~/.gentle-shell/agent/APPEND_SYSTEM.md`; its prior contents are unknown and were not restored. The same failure reproduced on HEAD; subsequent package checks passed with the override unset and external-file metadata unchanged. Next: commit P2, then map/implement P3–P4. Separate meaningful behavior in work-unit commits with tests. Preserve unrelated pre-existing untracked files.
